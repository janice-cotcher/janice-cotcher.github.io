def average_Mark(per1, per2, per3, per4, per5):
  """
  This function takes 5 marks and calculates your average grade
  """
  return (per1 + per2 + per3 + per4 + per5)/5


print(average_Mark(55, 65, 78, 83, 89))