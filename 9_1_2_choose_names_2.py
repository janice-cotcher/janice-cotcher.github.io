# not recommended
def ar():
  """Calculate the area of rectangle with dimensions l and w"""
  l = float(input("Input the length of the rectangle: "))
  w = float(input("Input the width of the rectangle: "))
  A = l * w
  print("The area is " + str(A) + "cm^2")


ar()